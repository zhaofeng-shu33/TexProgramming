\documentclass[type=bachelor]{thuthesis}
% 选项：
%   type=[bachelor|master|doctor|postdoctor], % 必选
%   secret,                                   % 可选
%   pifootnote,                               % 可选（建议打开）
%   openany|openright,                        % 可选，基本不用
%   arial,                                    % 可选，基本不用
%   arialtoc,                                 % 可选，基本不用
%   arialtitle                                % 可选，基本不用

% 所有其它可能用到的包都统一放到这里了，可以根据自己的实际添加或者删除。
\usepackage{thuthesis}
\usepackage{bm}
\newcommand{\transpose}[1]{\ensuremath{#1^{\scriptscriptstyle T}}}
\DeclareMathOperator*{\rgmax}{argmax}
\DeclareMathOperator*{\rgmin}{argmin}
\DeclareMathOperator{\Tr}{Tr}
% 定义所有的图片文件在 figures 子目录下
\graphicspath{{figures/}}

% 可以在这里修改配置文件中的定义。导言区可以使用中文。

\begin{document}

%%% 封面部分
\frontmatter
\input{data/myCover}
% 如果使用授权说明扫描页，将可选参数中指定为扫描得到的 PDF 文件名，例如：
\makecover

%% 目录
\tableofcontents

%% 符号对照表
\input{data/myDenotation}


%%% 正文部分
\mainmatter
\include{data/myChap01Intro}
\include{data/myChap02Content}
\include{data/myChap03Content}
\include{data/myChap04Content}
\include{data/myChap05Content}%conclusion
%%% 其它部分
\backmatter

%% 本科生要这几个索引，研究生不要。选择性留下。
% 插图索引
\listoffigures
%\listofequations


%% 参考文献
% 注意：至少需要引用一篇参考文献，否则下面两行可能引起编译错误。
% 如果不需要参考文献，请将下面两行删除或注释掉。
\bibliographystyle{thuthesis-numeric}
\bibliography{ref/refs}


%% 致谢
\include{data/myAck}

%% 附录
\begin{appendix}
\input{data/myAppendix}
\end{appendix}


%% 本科生进行格式审查是需要下面这个表格，答辩可能不需要。选择性留下。
% 综合论文训练记录表
\includepdf[pages=-]{scan-record.pdf}
\end{document}
